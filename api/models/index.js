const userModel = require('./userModel');
const tokenBlacklistModel = require('./tokenBlacklistModel');
const quizModel = require('./quizModel');
const randomModel = require('./random');
const forumModel = require('./forum');

module.exports = {
    userModel,
    tokenBlacklistModel,
    quizModel,
    randomModel,
    forumModel
}